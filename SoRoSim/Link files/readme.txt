This folder contains the Link class which allows the user to define
different types of links as well as its methods: 
The PropUpdate Function which allows the user to update and modify the 
property values as required after the link is created 
And the GaussQuadrature function which finds the points and the weights
for any number of gaussian points the user selects
